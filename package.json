{
  "name": "tw-cc-sender",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test && node scripts/check-syntax.mjs",
    "check": "npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
